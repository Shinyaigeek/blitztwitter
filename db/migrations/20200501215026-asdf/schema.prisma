datasource sqlite {
  provider = "sqlite"
  url = "***"
}

generator client {
  provider = "prisma-client-js"
}

// --------------------------------------
// model Project {
// id        Int      @default(autoincrement()) @id
// name      String
// }
model User {
  id       Int    @default(autoincrement()) @id
  name     String
  mail     String
  img      String
  password String
}